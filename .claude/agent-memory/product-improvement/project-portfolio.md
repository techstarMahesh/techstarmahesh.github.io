---
name: pi-portfolio
description: Recruiter-lens review status and open improvement items for the portfolio
metadata:
  type: project
---

**Lens:** review as a technical recruiter / hiring engineer skimming for 30s. Label every suggestion [must-have] or [nice-to-have]. Respect the static-only constraint.

**Already applied:**
- og:image + 1200×630 `og-cover.png`, `summary_large_image`, canonical URL, JSON-LD Person, sitemap.xml + robots.txt, `defer` scripts.
- Hero: "3+ years" experience line + "Available for backend roles" status pill.

**Top remaining must-have:**
- **Quantify project impact** in `projects.js` — only Zippee has a metric (5s→1s). For a backend role, latency/throughput/scale/reliability numbers are the key signal. Waiting on real numbers from Mahesh; do not fabricate.

**Declined by user (do not re-push unless asked):** self-host résumé PDF (vs Google Drive link), drop Google Fonts for system stack, visual polish (stronger featured-card border at rest, lighter social buttons so résumé CTA wins, "core stack" emphasis in Skills, reduced mobile section padding, external-link ↗ affordance).

**Possible future nice-to-haves not yet raised:** live-product links for publicly visible projects (SuperTails, TEN X YOU) as proof; project glyphs/icons to break up the text-dense grid.
